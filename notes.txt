What apps do I need?

User
Groceries
Units
Food
Diary
Recipes
Statistics
Settings
Units
About

# Create an app
python manage.py startapp <app name>

# Run server
python manage.py runserver

# Sample User
user: Merijn
password: Following5_Green7_kitchen3

# Migration shell
python manage.py shell
from body_metrics import BodyMetric
metric = BodyMetric.objects.get(name="Height")
metric.delete()

# Make migrations when they are not made yet.
python manage.py makemigrations

# Update migrations "units", then apply to database
python manage.py makemigrations
python manage.py migrate

What I'll do today:
* I want to generate a mealplan
  * goals need to be implemented
  * scripts need to be implemented
  * recipes may need to be edited.
* I want a graph of my macros and weight
  * goals need to be implemented


TODO LIST

General:
* website looks ugly
* There is no way to ender data for height.

Diary:
* quick add foods instead of sepparate page.
* add recipes, not just individual foods.
* Default meals, defined in settings.
* A time range for when the meals are eaten.
* edit entries for the meals, its not possible now.

Settings:
* Rename to user settings.
* look into user settings from django itself.

# Scripting language function ideas:

add_food
{food:kwark}
amount {500}
to {default_meal:breakfast}
for {next_7_days}/{tomorrow}/{today}
starting: {next_monday}/next_sunday/{Now}
contraint {meal_is_empty}/{None}

Name: [mealplan #1] <-- textbox

[funcions dropdown menu] [add function button] <-- gets added to this page when pressed.

set_goals
nutrient:protein = 26.4 * body_metric:weight
nutrient:fats = 0.8 * body_metric:weight
nutrient: carbs = 1.6 * body_metric:weight
nutreint: kcals = 3.2 * body_metric:weight
start_date = now
end_date = next_week

Functions:
add_food_to_diary {food: kwark} <-- food picker gets opened when pressed.
amount {default|1} servings {1} <-- text boxes can be changed.
to {default_meal: breakfast} <-- dropdown menu
for {next_7_days | tomorrow | today | in_4_days} <-- dropdown menu
starting {next_monday | next_sunday | now} <-- dropdown menu
constraint {meal_is_empty | none} <-- dropdown menu

list_a {recipe: tacos} {recipe: burritos} {recipe: pasta}
random(list_a)

add_recipe_to_diary {recipe: carbonara} | random(list_a)
how {fit_nutrition_goal_percentages | ideal_amounts}
amount {fourth_of_daily_intake}
to {default_meal: dinner}
for {next_7_days | tomorrow | today | in_4_days}
starting {next_monday | next_sunday | now}
constraint {meal_is_empty | none}

create_grocery_list
next_7_days

delete_grocery_list
before_today